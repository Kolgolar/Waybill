Репозиторий с проектом: https://github.com/Kolgolar/Waybill.git
Моя почта: ovniy@yandex.ru

Логин админки: admin
Пароль: 12345

Есть несколько багов, которые не исправлены в локальном проекте, но уже могут быть исправлены в репозитории на гитхабе (master ветка):

1) Возможно, что после очистки БД главная страница откажется загружаться, в таком случае нужно просто добавить хотя бы один объект в справочник маршрутов,
в справочник групп расходов и в справочник подразделений через админку.
Ошибка скорее всего возникает в forms.py в классе WRideForm, ибо там создаётся 3 массива с данными из БД,
которые используются для подстановки данных в поля, и Питону не нравятся, что они пустые.

2) Ещё одна проблема: если пользователь введёт какое-то своё описание маршрута, то оно не сохранится, ибо
в модели поездки запоминается только его номер, и, в итоге в печатной форме будет выведено либо описание маршрута из БД,
либо ничего, если пользователь ввёл неизвестный маршрут. Плюс в самом html файле поле для описание маршрута создано собственно через хтмл, оно
не берётся из формы и не отправляет данные на сервер.

Короче говоря, нужно добавить в модель поездки описание маршрута, добавить это же в форму, и вывести это поле в хтмл.


А так всё должно работать)))
 